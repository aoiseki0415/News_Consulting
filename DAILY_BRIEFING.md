# 毎日配信用 指示書

日本時間の毎日8:00に、同じCodexプロジェクト内の「毎日ブリーフィング」スレッドへ以下を配信する。

予定済みタスクによって「プロジェクト内のREADMEを確認し、情報発信を行なってください」という指示が展開されたら、情報収集より先にプロジェクト直下の `README.md` を開く。続けて、READMEが必読として指定する `OPERATION_SPEC.md` と本ファイルを確認し、最新の設定と制約に従ってタスクを開始する。

## 実行前チェック

1. `README.md`、`OPERATION_SPEC.md`、`DAILY_BRIEFING.md` の3文書を確認済みであることを確かめる。
2. 現在日時と前日の日付をAsia/Tokyoで確定する。
3. Notion「就活ワークスペース」の「企業分析 → コンサル」から最新の対象企業一覧を取得する。
4. 配信時点でウェブを検索し、掲載候補の出典ページを実際に開く。
5. すべてのURL、公開日、発表主体、本文との整合性を確認する。
6. 公式発表を最優先し、確認できない内容を推測で補わない。
7. ウェブ検索や出典ページの確認ができない場合は、古い情報で代替せず利用者へ報告する。

## Computer Use実行前チェック

このプロジェクトでComputer Useを使用する必要が生じた場合は、対象アプリの状態取得や最初のUI操作より前に、必ず次のコマンドをバックグラウンドで実行する。

```sh
caffeinate -u -t 600 &
```

この処理により、ディスプレイがオフの場合はオンになり、600秒間はユーザー操作中としてアイドルによるディスプレイスリープが抑止される。コマンド実行後は、ディスプレイが起きていることを確認してからComputer Useを開始する。確認できない場合はComputer Useを開始せず、その段階で利用者へ状況を報告する。ディスプレイの起動、起動状態の確認、Computer Useの開始には利用者の承認を求めない。Computer Useが600秒を超える場合は、次のUI操作より前に同じコマンドを再実行する。このコマンドはMacのロックを解除しないため、すでにロックされていて操作できない場合は利用者へ報告する。

## 月曜日の週次復習

Asia/Tokyoで月曜日の場合は、通常の情報収集とA・Bの配信より先に以下を実行する。

1. 直前の月曜日から日曜日までを前週として確定する。
2. Notion「ビジネスフレームワーク一覧」のデータソース（`collection://3ba161f2-8097-80e5-89d6-000ba0582999`）から、`日付` が前週に含まれる既存ページを取得する。
3. 各ページの内容を読み、フレームワーク名と概念・用途を1〜2文にまとめる。
4. 次のような短い箇条書きで復習を配信する。

   - フレームワーク名：内容の簡潔な要約

5. 該当する記録がない場合は、その旨を明記する。
6. 復習だけを提示し、利用者の確認を待つ。この時点では通常ブリーフィングを続けて配信しない。
7. 利用者が確認できたことを示したら、「では次に、今日のブリーフィングを行いますか？」と問いかける。
8. 利用者から「お願いします」などの明示的な開始依頼を受けた場合に限り、通常のA・Bブリーフィングを開始する。

返答がない場合、開始しない旨の返答を受けた場合、または開始依頼が曖昧な場合は、通常ブリーフィングへ進まない。復習で参照するNotionの既存ページは読み取り専用とし、編集しない。月曜日以外はこのフローを省略する。

## 出力フォーマット

### A. 総合コンサル・ITコンサル・SIer業界情報

#### 1. ファームの最新動向（4件）

各項目に以下を含める。

- 企業名・見出し
- 公開日
- 要点
- 就活・企業研究上の注目点
- 出典名とURL

原則として直近1か月以内の公式プレスリリースを中心に、重要度の高い4件を選ぶ。

#### 2. 前日の新卒採用情報（該当するものすべて）

各項目に以下を含める。

- 企業名・募集／イベント名
- 前日に公開されたことを確認できる日付
- 対象者、開催日、応募期限などの要点
- 公式の出典名とURL

前日以外に公開された情報を混ぜない。該当がない場合は、その旨を明記する。

### B. 毎日の学習

#### 1. 今日のビジネスフレームワーク

- 名称
- 概念
- 使える場面
- GD／ケース面接での使い方
- 注意点

既出テーマとの重複を避ける。

#### 2. 今日のGD／ケース問題

- テーマ
- 前提条件
- 分析のヒント

当日のフレームワークを使いやすい問題を優先し、模範解答はまだ提示しない。利用者に `What → Why → How` を意識した大まかな回答方針を求める。

## 回答後のフィードバック

利用者から回答方針を受け取ったら、構造化、仮説、原因分析、真因、施策とのつながり、優先順位、実現性の観点から具体的に返す。

## 対話完了後

利用者がその日のやり取りの完了を示したら、「本日の内容をGoogleスライドにまとめますか？」と問いかける。この確認だけではスライドを作成・変更しない。

利用者から「スライドを作成してください」などの明示的な依頼を受けた場合に限り、マイドライブ内の「コンサル毎日ブリーフィング」フォルダ（ID：`13Jpr1sExTpXQWFNN-5vfSvj0Kb3MZBU0`）にある `YYYY年MM月` 形式の当月ファイルへ、末尾に新しいスライドを必ず1枚追加する。当月のファイルがまだなければ、このフォルダ内に新規作成する。日付、AとBの要点、演習への回答・フィードバック、主要な出典を、新しく追加したスライドだけに読みやすい分量でまとめる。返答がない場合や作成しない旨の返答を受けた場合は、スライドを変更しない。

スライド作成時は、同フォルダ内の「毎日ブリーフィングスライドテンプレ」（ID：`1nTHoTCqsyQXlTZRLwZFiJfQdKHNWO7Ue3eiZk3PPD-0`）を必ず参照する。テンプレートのレイアウト、配色、フォント、文字サイズ、余白、見出し、出典欄などを踏襲し、内容量と役割に合うスライド構成を選ぶ。テンプレート原本は編集、移動、名称変更、共有設定変更、削除しない。当月ファイルがない場合は、可能な限りテンプレートのコピーとして作成する。

書き込み前後に当月ファイルのスライド一覧を取得する。書き込み対象は新しく作成または複製したスライドIDだけに限定し、既存スライドを上書き、置換、削除、並べ替えしない。作成後、既存スライドが保持され、末尾に1枚増えたことを確認する。失敗時に既存スライドへ代替記入しない。

スライドの文字は黒字を基本とし、重要な結論、語句、期限、注意点だけを赤字で強調する。赤字を装飾目的で多用しない。

Google Driveに対する検索、閲覧、作成、編集、移動、共有、削除などの操作は、上記フォルダと、その中に本プロジェクトが作成したファイルだけに限定する。指定フォルダ外は操作しない。保存先を再検索せず、確定済みのフォルダIDを使用する。

システム上、月次Googleスライドを指定フォルダ内へ直接作成できない場合に限り、マイドライブ直下へ一時作成してよい。作成処理から返されたファイルIDだけを対象に、現在の親フォルダを確認し、直後に指定フォルダへ移動する。移動後は同じファイルIDのメタデータを再取得し、親フォルダが `13Jpr1sExTpXQWFNN-5vfSvj0Kb3MZBU0` であることを必ず確認する。この処理では作成したファイル以外を操作しない。移動または検証に失敗した場合は、成功と報告しない。

## Notionへのフレームワーク記録

当日のビジネスフレームワークは、Notion「就活ワークスペース」内の「ビジネスフレームワーク一覧」にあるテーブルへ毎日1件追加する。

- 親ページID：`3ba161f2-8097-8042-8b7f-f53cd8290d19`
- データソースID：`collection://3ba161f2-8097-80e5-89d6-000ba0582999`
- `名前`：当日のフレームワーク名
- `日付`：日本時間の配信日

作成する新規ページの本文には、当日の情報発信で説明した概念・構成要素、利用場面、GD／ケース面接での使い方、注意点、例を記載する。書き込み直前に親ページとデータソースを取得してスキーマを確認し、作成後は新規ページを読み戻して保存結果を確認する。

Notionで編集してよいのは、このデータソースへの新規ページ作成と、その処理で新規作成したフレームワークページだけとする。親ページ本体、既存項目、就活ワークスペース内のその他すべてのページやデータベースは読み取り専用とし、編集、作成、移動、削除しない。対象を確認できない場合は、別の場所へ書き込まない。

## 一連の工程終了後のAppleリマインダー処理

Googleスライドの作成と保存結果の検証まで完了し、一連の工程が終了したら、利用者へ正確に「リマインダーを実行済みにして良いですか」と尋ねる。この確認だけではApple純正の「リマインダー」アプリを操作しない。

利用者から対象リマインダーを実行済みにする明示的な許可を受けた場合に限り、先に `caffeinate -u -t 600 &` を承認確認なしで実行し、ディスプレイが起きていることを確認してからComputer Useを開始する。ディスプレイを起こせない場合は、リマインダーを操作せず、その時点で利用者へ報告する。ディスプレイが起きている場合は、Apple純正の「リマインダー」アプリにある `Daily To Do` リスト内の「コンサルブリーフィング」だけを実行済みにする。許可されるリマインダー操作はこれだけとし、新規作成、名称・日時・繰り返し・メモ・優先度・リスト等の編集、移動、削除、復元、並べ替え、または他のリマインダーへの操作は行わない。

`Daily To Do` リストまたは「コンサルブリーフィング」を確認できない場合は、似た名称の項目や別のリストを操作せず、その旨を利用者へ報告する。実行済みにした後は、対象項目が実行済みになったことだけを確認し、それ以外の変更を行わない。
