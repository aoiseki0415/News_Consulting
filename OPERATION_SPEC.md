# 運用仕様

## 1. 目的

総合コンサル、ITコンサル、SIerを中心とした最新情報の収集と、GD／ケース面接対策の継続学習を1つのデイリーフローとして運用する。

## 2. 日次フロー

1. 日本時間の毎日8:00に、同じCodexプロジェクト内の「毎日ブリーフィング」スレッドで予定済みタスクを実行する。
2. スレッド内に「プロジェクト内のREADMEを確認し、情報発信を行なってください」という指示が展開されたら、最初にプロジェクト直下の `README.md` を確認する。
3. READMEの指示に従い、`OPERATION_SPEC.md` と `DAILY_BRIEFING.md` も確認する。
4. 月曜日の場合は「月曜日の週次復習フロー」を完了し、利用者から通常ブリーフィングの明示的な開始依頼を受けるまで待つ。
5. 通常ブリーフィングの開始後、Notionの対象企業一覧を取得する。
6. 配信時点でウェブ検索を実行し、業界情報を調査して出典付きで配信する。
7. 当日のフレームワークと演習問題を配信する。
8. 利用者の回答を待ち、フィードバックする。
9. 当日の往復が完了したと利用者が示した後、Googleスライドを作成するか問いかける。
10. 利用者から明示的な作成依頼を受けた場合に限り、当月ファイルの末尾へ新しいスライドを1枚追加し、その新規スライドに内容を要約する。
11. 当日のビジネスフレームワークを、指定されたNotionテーブルへ新規ページとして1件追加する。
12. Googleスライドの作成と保存結果の確認まで含む一連の工程が完了したら、正確に「リマインダーを実行済みにして良いですか」と利用者へ確認する。
13. 利用者から明示的な許可を受けた場合に限り、Apple純正の「リマインダー」アプリにある `Daily To Do` リスト内の「コンサルブリーフィング」を実行済みにする。

スライド作成は利用者の回答前には行わない。また、対話完了後も利用者の明示的な作成依頼を受けるまでは実行しない。対話が複数回続く場合は、最終的なフィードバックまで反映する。

## 3. 調査条件

### 3.0 リアルタイム情報の取得

- 毎回、配信実行時点でウェブ検索を行う。モデルの既存知識だけで業界情報や採用情報を生成しない。
- 検索結果の要約だけで判断せず、掲載する出典ページを実際に開いて内容と公開日を確認する。
- 企業公式サイト、公式プレスリリース、公式採用ページ、公式SNSなどの一次情報を優先する。
- 各項目に参照URLを付け、公開日と出来事の発生日を区別する。
- ウェブ検索、ページ閲覧、公開日確認のいずれかを実行できない場合は、鮮度を保証できない情報を配信せず、利用者へ理由を報告する。

### 3.1 対象企業

配信時点のNotion「就活ワークスペース」にある「企業分析 → コンサル」の企業一覧を正とする。固定リストを別途保持せず、企業の追加・削除を毎回反映する。

### 3.2 ファームの最新動向

- 掲載数は必ず4件とする。
- 原則、配信日から遡って1か月以内に公開された情報を調査する。
- 公式プレスリリース、公式ニュースルーム、公式発表を優先する。
- 事業戦略、大型提携、買収、新サービス、調査発表、組織・経営の重要な変更など、就活上の企業理解に役立つ情報を優先する。
- 同じ企業や同じ話題への過度な偏りを避ける。

### 3.3 新卒採用情報

- 日本時間で「配信日の前日 0:00〜23:59」に新規公開された情報だけを対象とする。
- 対象は新卒向けインターン、本選考、説明会、セミナー、採用イベント、応募期限など。
- 対象企業の公式サイト、公式採用サイト、公式SNSを優先して確認する。
- 更新日が確認できない情報や、前日より前に公開された情報は掲載しない。
- 該当情報は件数を制限せず、確認できたものをすべて掲載する。
- 該当がない場合は「前日公開の新卒採用情報は確認できませんでした」と明記する。

## 4. 学習条件

### 4.1 ビジネスフレームワーク

毎日1つを選び、次の順で説明する。

1. フレームワーク名
2. 概念・構成要素
3. 有効な場面
4. GD／ケース面接での具体的な使い方
5. 使用時の注意点

既出テーマを記録し、同じ内容の短期間での重複を避ける。

### 4.2 月曜日の週次復習フロー

日本時間で月曜日に予定済みタスクが始まった場合、通常ブリーフィングより先に次の処理を行う。

1. Asia/Tokyoを基準に、直前の月曜日 0:00から日曜日 23:59までを前週の対象期間として確定する。
2. Notion「ビジネスフレームワーク一覧」のデータソースから、`日付` が対象期間に含まれる既存ページを取得する。
3. 各ページの本文を読み、次の形式で簡潔に復習を提示する。
   - フレームワーク名：概念・用途を1〜2文で要約
4. 復習は箇条書きとし、当時の説明を長文で転載しない。
5. 対象期間に記録がない場合は、推測で補わず「前週に記録されたフレームワークはありません」と伝える。
6. 復習を提示した後、利用者が内容を確認したことを示す返答を待つ。
7. 確認できたら、正確に「では次に、今日のブリーフィングを行いますか？」と問いかける。
8. 利用者から「お願いします」などの明示的な開始依頼を受けた場合に限り、A・Bの通常ブリーフィングを開始する。

復習提示と通常ブリーフィングを同じメッセージで連続配信しない。確認への返答がない場合、拒否された場合、または開始依頼が曖昧な場合は、通常ブリーフィングへ進まない。復習に利用する既存Notionページは読み取り専用とし、編集しない。

### 4.3 GD／ケース面接

- 毎日1題を提示する。
- 可能な限り、当日のフレームワークを自然に活用できるテーマとする。
- 問題文と分析のヒントまでを提示し、最初から模範解答は出さない。
- 利用者の回答方針に対し、次の観点でフィードバックする。
  - 論点の構造化
  - 前提と仮説の妥当性
  - `What`：問題の特定
  - `Why`：原因分析と真因・課題の特定
  - `How`：施策と原因のつながり
  - 優先順位、実現性、効果検証

## 5. 出典表記

ニュースごとに以下を記載する。

- 情報タイトル
- 発表主体
- 公開日
- 要約
- 参照URL

二次情報を利用する場合も、可能な限り一次情報を併記する。公開日と出来事の発生日が異なる場合は混同しない。

## 6. スライド記録

### 保存先と操作範囲

- 対象アカウント：`sekiaoi0818@keio.jp`
- 保存先：マイドライブ内「コンサル毎日ブリーフィング」
- フォルダID：`13Jpr1sExTpXQWFNN-5vfSvj0Kb3MZBU0`
- フォルダURL：<https://drive.google.com/drive/folders/13Jpr1sExTpXQWFNN-5vfSvj0Kb3MZBU0>

### 指定テンプレート

- テンプレート名：`毎日ブリーフィングスライドテンプレ`
- テンプレートID：`1nTHoTCqsyQXlTZRLwZFiJfQdKHNWO7Ue3eiZk3PPD-0`
- テンプレートURL：<https://docs.google.com/presentation/d/1nTHoTCqsyQXlTZRLwZFiJfQdKHNWO7Ue3eiZk3PPD-0/edit?usp=drivesdk>

月次Googleスライドの新規作成および毎日1枚のスライド追加では、このテンプレートを必ず参照する。作成前にテンプレートの構造と視覚表現を確認し、レイアウト、配色、フォント、文字サイズ、余白、見出し、出典欄などを踏襲する。テンプレート原本は読み取り専用の参照元として扱い、編集、移動、名称変更、共有設定変更、削除を行わない。

月次ファイルは、可能な場合はテンプレートのコピーとして作成する。日次スライドは、内容量と役割に合うテンプレート内のスライドまたはレイアウトを選び、構造を保ったまま内容を差し替える。テンプレートに収まらない場合も、文字を過度に縮小せず、内容を短く整理する。

Google Driveに対するすべての操作は、このフォルダと、その中に本プロジェクトが作成したファイルだけに限定する。指定フォルダ外のファイルやフォルダを検索、閲覧、作成、編集、移動、共有、削除しない。保存先を名前から再検索せず、上記の確定済みフォルダIDまたはURLを使用する。

ただし、システム上、ネイティブGoogleスライドを指定フォルダ内へ直接作成できない場合は、次の手順に限り例外として許可する。

1. マイドライブ直下へ月次Googleスライドを一時作成する。
2. 作成処理から返されたファイルIDを記録する。
3. そのファイルIDのメタデータだけを読み、現在の親フォルダを確認する。
4. 作成したファイルだけを、直後に指定フォルダID `13Jpr1sExTpXQWFNN-5vfSvj0Kb3MZBU0` へ移動する。
5. 同じファイルIDのメタデータを再取得し、親フォルダが指定フォルダIDになったことを確認する。

この例外手順中も、作成したファイル以外を検索、閲覧、編集、移動、共有、削除しない。移動または検証に失敗した場合は、成功したものとして扱わず利用者へ報告する。

### 作成前の確認

1. 当日の情報発信、回答、フィードバックが完了した後、「本日の内容をGoogleスライドにまとめますか？」と利用者へ確認する。
2. 利用者から「スライドを作成してください」などの明示的な依頼を受ける。
3. 明示的な依頼を受けた場合に限り、当月ファイルへの追加または月初の新規ファイル作成を行う。

確認への返答がない場合や、作成しない旨の返答を受けた場合は、Googleスライドを変更しない。

### ファイル管理

- ファイルは1か月につき1つ作成する。
- ファイル名：`YYYY年MM月`（年月以外の文言を付けない）
- 月初または当月最初の作成依頼時に、指定テンプレートを参照し、指定フォルダ内へ当月のGoogleスライドを1つ作成する。
- 月内は同じファイルの末尾へ、日付順に毎日必ず新しいスライドを1枚追加する。
- 月が変わった最初の記録で新規ファイルを作成する。

### 追記・上書き防止

1. 作成前に当月ファイルのスライド一覧と既存スライドIDを取得する。
2. 当日の記録用として新しいスライドを1枚作成またはテンプレートから複製する。
3. 書き込み対象を新しく作成したスライドIDに限定する。
4. 既存スライドのテキスト、レイアウト、順序を上書き、置換、削除しない。
5. 作成後にスライド一覧を再取得し、既存スライドが保持され、末尾に新しいスライドが1枚増えたことを確認する。

新規スライドの追加または検証に失敗した場合は、既存スライドへ代替記入せず、成功として扱わない。

### 文字色

- 本文、見出し、出典は黒字を基本とする。
- 赤字は重要な結論、重要語句、期限、特に注意すべき点など、強調が必要な箇所だけに限定する。
- 装飾目的で赤字を多用せず、1枚の中で強調点が判別できる量に抑える。

### 1枚の構成

- タイトル：日付
- 業界動向：重要ニュースと採用情報の要点
- 今日のフレームワーク：名称と要旨
- GD／ケース：問題、回答方針、主要フィードバック
- 出典：短縮表示した発表主体・タイトル・URL

可読性を優先し、詳細な会話の転記は避ける。URLはリンクとして保持する。

## 7. Notionへのフレームワーク記録

### 保存先

- 親ページ：`ビジネスフレームワーク一覧`
- 親ページID：`3ba161f2-8097-8042-8b7f-f53cd8290d19`
- 親ページURL：<https://app.notion.com/p/3ba161f2809780428b7ff53cd8290d19>
- テーブルのデータソースID：`collection://3ba161f2-8097-80e5-89d6-000ba0582999`
- 確認済みプロパティ：`名前`（タイトル）、`日付`（日付）

### 毎日の追加方法

1. 書き込み直前に親ページとデータソースを取得し、ID、親子関係、最新スキーマを確認する。
2. 当日のフレームワーク名を `名前`、日本時間の配信日を `日付` として、新しいページをテーブルへ1件作成する。
3. 新規ページ本文に次の内容を記載する。
   - 概念と構成要素
   - 有効な利用場面
   - GD／ケース面接での具体的な使い方
   - 使用時の注意点
   - 当日の情報発信で使用した説明や例
4. 作成したページを読み戻し、プロパティと本文が保存されたことを確認する。

### 編集権限の境界

Notionで書き込みを許可する対象は、上記データソースへの新規ページ作成と、そこで新規作成したフレームワークページの本文・プロパティだけに限定する。「ビジネスフレームワーク一覧」の親ページ本体、既存のテーブル項目、就活ワークスペース内のその他のページやデータベースは編集、作成、移動、削除しない。

業界情報の対象企業を特定するために他のNotionページを検索・閲覧することは許可するが、読み取り専用とする。対象ページまたはデータソースを確認できない場合は、別の場所へ書き込まず利用者へ報告する。

月曜日の週次復習では、上記データソースの前週分の既存ページを検索・閲覧してよい。ただし、既存ページおよびそのプロパティは読み取り専用とし、復習のための編集、追記、移動、削除を行わない。

## 8. Appleリマインダーの完了処理

### Computer Use実行前のディスプレイ維持

Apple純正の「リマインダー」アプリを含め、このプロジェクトでComputer Useを使用する場合は、最初のアプリ状態取得やUI操作より前に、ターミナルで次のコマンドを必ず実行する。

```sh
caffeinate -u -t 600 &
```

- `-u` によりユーザーが操作中であることを宣言し、ディスプレイがオフの場合はオンにして、アイドルによるディスプレイスリープを防ぐ。
- `-t 600` により、このアサーションを600秒間維持する。
- `&` により、Computer Useと並行できるようバックグラウンドで実行する。
- Computer Useが600秒を超えて続く場合は、600秒の経過前または次のUI操作より前に同じコマンドを再実行する。
- このコマンドはログイン画面のロックを解除しない。実行後もMacがロックされていてComputer Useを実行できない場合は、ロック解除や別手段による操作を試みず利用者へ報告する。

### 実行条件

1. Googleスライドの作成と、既存スライドの保持・末尾への1枚追加の検証まで完了する。
2. 一連の工程の終了後、利用者へ正確に「リマインダーを実行済みにして良いですか」と尋ねる。
3. 利用者から明示的な許可を受けるまで、Apple純正の「リマインダー」アプリを操作しない。
4. 許可を受けた場合に限り、`Daily To Do` リストを確認し、その中の「コンサルブリーフィング」を実行済みにする。

### 操作範囲

- 許可される操作は、上記の対象リマインダーを実行済みにすることだけとする。
- 新しいリマインダーの作成、名称・日時・繰り返し・メモ・優先度・リスト等の編集、移動、削除、復元、並べ替え、その他のリマインダーへの変更は行わない。
- `Daily To Do` リストまたは「コンサルブリーフィング」を確認できない場合、似た名称の項目や別のリストを代替操作せず、利用者へ報告する。
- 実行済みにした後は、対象項目が実行済みになったことだけを確認し、追加の変更を行わない。

## 9. 未設定項目

- 「対話完了」を判断する利用者の合図

これらは利用者から指定を受けた時点で更新する。
